# For local dev the Vite proxy handles /api -> http://127.0.0.1:8000 automatically.
# For production (Vercel), set this to your deployed backend URL:
# VITE_API_BASE=https://xlb-backend.onrender.com
VITE_API_BASE=/api
